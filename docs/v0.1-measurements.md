# v0.1 measurements

Measured 2026-08-27 with the Linux/arm64 Go toolchain in this build environment and `CGO_ENABLED=0`:

| Metric | Result |
|---|---:|
| stripped runtime binary used for smoke test | 17,661,882 bytes |
| fresh daemon readiness | 18.5 ms |
| idle RSS | 15,417,344 bytes |
| cron next-fire evaluation | 718 ns/op (about 0.72 ms for 1,000 definitions) |
| sustained 1 KiB tagged/zstd frame ingestion | 47.5 MB/s |

Commands are the `BenchmarkNextFire` and `BenchmarkFrameEncoding` benchmarks plus a fresh `minicron init` daemon smoke test. Results are goals and snapshots, not contractual limits; release CI should retain platform-specific measurements.
