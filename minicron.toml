[server]
bind = "127.0.0.1:7423"
unix_socket = true

[include]
paths = ["jobs/*.toml"]

[scheduler]
timezone = "UTC"
max_concurrent_runs = 32

[logs]
backend = "file"

[defaults]
shell = "/bin/sh"
env_base = "clean"
grace = "10s"
