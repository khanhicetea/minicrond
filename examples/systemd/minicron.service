[Unit]
Description=minicrond user daemon
After=network.target

[Service]
Type=simple
ExecStart=%h/.local/bin/minicrond daemon --config %h/.config/minicron/minicron.toml
Restart=on-failure
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=default.target
